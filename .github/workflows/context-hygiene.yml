# SPDX-FileCopyrightText: 2026 Libre AI contributors
# SPDX-License-Identifier: Apache-2.0
name: Context hygiene

# Fleet invariant: blocks private identifiers and machine-local paths from
# entering the public tree (reusable governed workflow, pinned by commit).
on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  context-hygiene:
    uses: libre-ai/governance/.github/workflows/reusable-context-hygiene.yml@8c238c09efd443a4866459160dac1cfee31d88be
